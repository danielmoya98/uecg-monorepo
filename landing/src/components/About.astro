---
// FeaturesMosaic.astro
import { Image } from 'astro:assets';

// Definimos las imágenes como constantes para mantener el template limpio
const mainImage = "https://images.unsplash.com/photo-1571260899304-425eee4c7efc?q=80&w=1000&fit=crop";
const floatImage = "https://images.unsplash.com/photo-1544717305-2782549b5136?q=80&w=600&fit=crop";
---

<section class="py-24 bg-gray-50 overflow-hidden">
    <div class="container mx-auto px-6">
        <div class="grid lg:grid-cols-12 gap-12 items-center">

            <div class="lg:col-span-5 order-2 lg:order-1">
                <span class="text-[#000089] font-bold uppercase tracking-widest text-xs mb-4 block">
                    Sobre Nosotros
                </span>

                <h2 class="text-4xl lg:text-5xl font-bold text-gray-900 mb-6 leading-tight">
                    Construyendo <br>
                    el <span class="bg-[#000089] text-white px-2 italic font-serif">carácter</span> de los líderes.
                </h2>

                <p class="text-gray-600 text-lg mb-8 leading-relaxed">
                    La educación no es solo llenar un cubo, sino encender un fuego. En la U.E. Che Guevara, encendemos la pasión por aprender a través de la curiosidad y la práctica.
                </p>

                <div class="space-y-6">
                    <div class="flex items-start gap-4 group">
                        <div class="w-12 h-12 rounded-full border border-gray-200 bg-white flex items-center justify-center text-[#000089] font-bold text-lg shrink-0 group-hover:bg-[#000089] group-hover:text-white transition-colors duration-300">1</div>
                        <div>
                            <h4 class="font-bold text-gray-900 text-lg">Atención Personalizada</h4>
                            <p class="text-sm text-gray-500 mt-1">Grupos reducidos para un mejor seguimiento académico.</p>
                        </div>
                    </div>

                    <div class="flex items-start gap-4 group">
                        <div class="w-12 h-12 rounded-full border border-gray-200 bg-white flex items-center justify-center text-[#000089] font-bold text-lg shrink-0 group-hover:bg-[#000089] group-hover:text-white transition-colors duration-300">2</div>
                        <div>
                            <h4 class="font-bold text-gray-900 text-lg">Infraestructura Segura</h4>
                            <p class="text-sm text-gray-500 mt-1">Monitoreo constante y control de acceso digital.</p>
                        </div>
                    </div>
                </div>
            </div>

            <div class="lg:col-span-7 order-1 lg:order-2 relative h-[500px] w-full">

                <div class="absolute top-0 right-10 w-3/4 h-3/4 bg-[#000089] rounded-tr-[4rem] rounded-bl-[4rem] z-0"></div>

                <div class="absolute top-8 right-0 w-3/4 h-3/4 rounded-tr-[3rem] rounded-bl-[3rem] overflow-hidden shadow-2xl z-10 group">
                    <Image
                            src={mainImage}
                            alt="Estudiante Che Guevara"
                            width={800}
                    height={600}
                    format="webp"
                    loading="lazy"
                    decoding="async"
                    class="w-full h-full object-cover group-hover:scale-105 transition duration-700 ease-out"
                    />
                </div>

                <div class="absolute bottom-0 left-0 w-1/2 h-1/2 rounded-tr-[2rem] rounded-bl-[2rem] overflow-hidden shadow-2xl border-4 border-gray-50 z-20 animate-float-slow">
                    <Image
                            src={floatImage}
                            alt="Clase interactiva"
                            width={400}
                            height={300}
                            format="webp"
                            loading="lazy"
                            decoding="async"
                            class="w-full h-full object-cover"
                    />
                </div>

                <div class="absolute top-1/2 left-10 w-24 h-24 border-4 border-dashed border-white/40 rounded-full animate-spin-slow z-10 pointer-events-none"></div>

            </div>

        </div>
    </div>
</section>

<style>
    @keyframes float-slow {
        0%, 100% { transform: translateY(0); }
        50% { transform: translateY(-15px); }
    }
    .animate-float-slow {
        animation: float-slow 6s ease-in-out infinite;
        will-change: transform; /* Aviso al navegador para optimizar GPU */
    }
    .animate-spin-slow {
        animation: spin 12s linear infinite;
    }
    @keyframes spin {
        from { transform: rotate(0deg); }
        to { transform: rotate(360deg); }
    }
</style>